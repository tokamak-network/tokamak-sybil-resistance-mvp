// Check if balance meets requirement
sufficient_balances[i] = balance_vector[i] >= required_balance ? 1 : 0;

// Create link matrix with i linked to j if they both have the required
// balance and both have vouched for each other
symmetric_vouch_matrix[i][j] = vouch_matrix[i][j]*vouch_matrix[j][i]
symmetric_balance_matrix[i][j] = sufficient_balances[i]*sufficient_balances[j]
link_matrix[i][j] = symmetric_vouch_matrix[i][j]*symmetric_balance_matrix[i][j]

// Compute the degree of each node using link matrix
degree_vector[i] = link_matrix[i][0]+link_matrix[i][1]+...+link_matrix[i][N-1]

// Check inverse_degree_vector is correct
inverse_degree_vector[i]*degree_vector[i] = D

// Compute the old score or node scaled by degree of node
scaled_old_score_vector[i] = inverse_degree_vector[i]*old_score_vector[i]

// New score for j is sum of these values for j where i linked to j
D*new_score_vector[i] = scaled_old_score_vector[0]*link_matrix[i][0]+...+scaled_old_score_vector[N-1]*link_matrix[i][N-1]
 
